# Ultra-Light Requirements for Azure Production

# PRODUCTION AZURE - Minimal Runtime Dependencies
# Total size: ~50MB (vs 2-3GB with ML models)

# Core Flask Dependencies
Flask==3.0.3
Flask-CORS==4.0.1
Werkzeug==3.0.1
gunicorn==21.2.0

# Essential utilities only
requests==2.31.0
python-dotenv==1.0.0
rapidfuzz==3.9.3

# Lightweight JSON/data handling
Jinja2>=3.1.2
MarkupSafe>=2.1.3
click>=8.1.3
itsdangerous>=2.1.2

# Note: SQLite3 is built into Python - no additional dependency needed
# Note: All ML libraries, PDF processing, and OCR removed for production
# These are handled in batch pre-processing phase